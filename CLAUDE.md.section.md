## specialist ensemble の使い方

claude-ensemble は adaptive specialist ensemble として使う。委譲は手段であって工程ではない。固定パイプラインを作らず、そのタスクで専門家を使う価値が明確な場合だけ委譲する。迷ったら委譲しない。

### 委譲しない
- 変更が小さく、意図と触る場所が明確なもの
- 文言・スタイル・設定値の変更
- 既に文脈に必要な情報が揃っているもの
- 「工程を埋めるため」「変更量が多いから」という理由だけの委譲

### specialist ごとの起動条件
- explore: 既存実装・呼び出し関係・配置を知らず、変更前の事実確認が必要なとき。検索と読み込みの往復が多く、結果の大半が判断に不要な調査を結論だけに圧縮したいとき。単に変更ファイルが多いだけでは呼ばない
- architect: API、schema、module boundary、責務分割、依存関係など、後から変えるコストが高い設計判断があるとき。変更量が多いだけでは呼ばない
- implementer: 互いに独立した実装を並列で進めるとき、または機械的変更を親の文脈から隔離したいとき。実装後は変更箇所に直接対応する最小限の検証まで。全体 test / build / typecheck は原則として任せない
- test-runner: test suite、build、全体 typecheck など重い検証をまとめて実行し、長いログを隔離したいとき
- reviewer: ロジック、状態管理、エラー処理、公開 API など非自明な変更を、コミット前に第三者視点で確認する価値があるとき。文言・スタイル変更では呼ばない

### 委譲を検討するサイン
- 同じ領域を何度も探索し直している、読むべきファイルの見当がつかない
- 判断に使わない長い出力（検索結果、ログ、ファイル全文）が文脈に溜まり続けている

### 運用原則
- specialist を呼ぶ順番は固定しない。不要な specialist は呼ばない
- specialist の出力は次の specialist を起動するための通過儀礼にしない
- state machine、強制 planner、agent counter は追加しない
- サブエージェントの「通ったはず」を検証結果の根拠にしない。終了コードか、自分で確認した結果を根拠にする
- specialist（サブエージェント）はコミットしない。変更内容とコミット案を返すまでにし、コミットはメインの会話で行う
